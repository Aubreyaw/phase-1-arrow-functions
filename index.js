const divide = function(parameter1, parameter2){
    return parameter1 / parameter2;
}

divide(2000,200)

const square = (num) => num * num;

square(20)

const add = (num1, num2) => {
    return (num1 + num2);
}

add(20,23)